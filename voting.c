#include <stdio.h>

int main()
{
    int x;
    printf("to find the member for voting eligiable are not");
    scanf("%d",&x);
    if(x>18)
    {
        printf("you can eligiable for voting");
    }
    else
    {
        printf("you are not eligiable for voting");
    }

    return 0;
}

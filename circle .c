#include <stdio.h>

int main()
{
    float r,c,d,a,l,m;
    printf("to find the circumference and area of the circle");
    scanf("%f",&a);
    scanf("%f",&l);
    scanf("%f",&m);
    a=180*r*r;
    l=2*r;
    m=c/(2*180);
    printf("%f %f %f",a,l,m);
    return 0;
}

